import React from "react";

/**
 * PUBLIC_INTERFACE
 * List/search jobs page.
 */
const Jobs = () => (
  <div>
    <h2>Job Listings</h2>
    <p>Job filters and available jobs will show here.</p>
  </div>
);

export default Jobs;
