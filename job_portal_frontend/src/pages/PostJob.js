import React from "react";

/**
 * PUBLIC_INTERFACE
 * Page for employer to post a new job.
 */
const PostJob = () => (
  <div>
    <h2>Post a New Job</h2>
    <p>Job posting form will go here.</p>
  </div>
);

export default PostJob;
